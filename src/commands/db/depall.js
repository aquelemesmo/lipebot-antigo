const Discord = require("discord.js")
const db = require("quick.db")

module.exports.run = async (bot,message,args) => {
    //undefined
}

module.exports.help = {
    name: "depall",
    aliases: ["dep-all"]
}